/**
 * 等于
 */
var ARY_STR_EQUAL = new Array("0"," = ? ","等于");

/**
 * 大于
 */
var ARY_STR_GREATER = new Array("1"," > ? ","大于");

/**
 * 小于
 */
var ARY_STR_LESS = new Array("2"," < ? ","小于");

/**
 * 包含
 */
var ARY_STR_LIKE = new Array("3"," Like ? ","包含");

/**
 * 介于
 */
var ARY_STR_BETWEEN = new Array("4"," Between ? And ? ","介于");

/**
 * 在……中
 */
var ARY_STR_INCLUDE = new Array("5"," In (?) ","在……中");

/**
 * 不等于
 */
var ARY_STR_NOTEQUAL = new Array("6"," <> ? ","不等于");

/**
 * 不在……中
 */
var ARY_STR_NOTINCLUDE = new Array("7"," Not In (?) ","不在……中");

/**
 * 并集
 */
var ARY_STR_UNION = new Array("8"," UNION ","并集");

/**
 * 不介于
 */
var ARY_STR_NOTBETWEEN = new Array("9"," Not Between ? And ? ","不介于");

/**
 * 不包含
 */
var ARY_STR_NOTLIKE = new Array("10"," Not Like ? ","不包含");

/**
 * “或连接”包含
 */
var ARY_STR_LIKE_OR = new Array("11"," Like ? ","包含（或连接）");
   
/**
 * 小于等于
 */
var ARY_STR_LESS_EQUAL = new Array("12"," <= ","小于等于");
   
/**
 * 大于等于
 */
var ARY_STR_GREATER_EQUAL = new Array("13"," >= ","大于等于");

/**
 * 空运算符
 */
var ARY_STR_NULLOPER = new Array("14","","空运算符");
/**
 * 不为空运算符
 */
var ARY_STR_ISNOTNULL = new Array("15","is not null","不为空运算符");